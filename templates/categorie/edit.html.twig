{% extends 'base.html.twig' %}

{% block title %}Edit Categorie{% endblock %}

{% block body %}
    <div style="text-align: center; padding: 20px;">
        <h1 style="border: 1px solid #000; display: inline-block; padding: 10px; text-transform: uppercase; text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);">Categorie</h1>
    </div>

        <table class="table">
            <thead>
                    <th>Nom</th>
                    <th>Actions</th>
                </tr>
            </thead>

            <tr>
                <td>{{ include('categorie/_form.html.twig', {'button_label': 'Update'}) }}</td>
                <td>
                    <div class="d-flex">
                    <!-- bouton update qui apparait -->
                        {# '{{ include('categorie/_form.html.twig', {'button_label': 'Update'}) }} '#}

                        <button class="btn btn-secondary" style="margin-left: 10px; height: 38px">{{ button_label|default('Update') }}</button>

                        {{ include('categorie/_delete_form.html.twig') }}
                        <a href="{{ path('app_categorie_index') }}" class="btn btn-primary" style="margin-left: 10px; height: 38px;">Back to list</a>
                    </div>
                </td>
            </tr>
        </table>
{% endblock %}
