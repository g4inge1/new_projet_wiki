{% extends 'base.html.twig' %}

{% block title %}Edit Categorie{% endblock %}

{% block body %}
    <h1 class="text-4xl font-bold mb-6 text-blue-800 text-center p-2 bg-blue-200 border border-blue-300 rounded-lg" style="box-shadow: 5px 5px 10px rgba(0, 0, 0, 0.2);">Edition Categorie</h1>
        <table class="table">
            <thead>
                    <th>Nom</th>
                    <th>Actions</th>
                </tr>
            </thead>

            <tr>
                <td>{{ include('categorie/_form.html.twig', {'button_label': 'Enregistrer'}) }}</td>
                <td>
                    <div class="d-flex">
                    <!-- bouton update qui apparait -->
                        {# '{{ include('categorie/_form.html.twig', {'button_label': 'Update'}) }} '#}

                        <button class="btn btn-secondary" style="margin-left: 10px; height: 38px">{{ button_label|default('Enregistrer') }}</button>

                        {{ include('categorie/_delete_form.html.twig') }}
                        <a href="{{ path('app_categorie_index') }}" class="btn btn-primary" style="margin-left: 10px; height: 38px;">Retour à la liste</a>
                    </div>
                </td>
            </tr>
        </table>
{% endblock %}
