{% extends 'base.html.twig' %}

{% block title %}New Categorie{% endblock %}

{% block body %}
    <h1 class="text-4xl font-bold mb-6 text-blue-800 text-center p-2 bg-blue-200 border border-blue-300 rounded-lg" style="box-shadow: 5px 5px 10px rgba(0, 0, 0, 0.2);">Nouvelle catégorie</h1>
    
    <div class="flex flex-col items-center justify-center">
        {{ include('categorie/_form.html.twig') }}

        <a href="{{ path('app_categorie_index') }}" class="btn btn-secondary">back to list</a>
    </div>
{% endblock %}
