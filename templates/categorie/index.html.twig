{% extends 'base.html.twig' %}

{% block title %}Categorie index{% endblock %}

{% block body %}

{#
    <div style="text-align: center; padding: 20px;">
        <h1 style="border: 1px solid #000; display: inline-block; padding: 10px;">Categorie index</h1>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for categorie in categories %}
                <tr>
                    <td>{{ categorie.id }}</td>
                    <td>{{ categorie.nom }}</td>
                    <td>
                        <a href="{{ path('app_categorie_show', {'id': categorie.id}) }}" class="btn btn-primary">Show</a>
                        <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="btn btn-secondary">Edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">No records found</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_new') }}" class="btn btn-secondary">Create new</a>

#}


    <div class="container">
        <h1 class="text-4xl font-bold mb-6 text-blue-800 text-center p-2 bg-blue-200 border border-blue-300 rounded-lg" style="box-shadow: 5px 5px 10px rgba(0, 0, 0, 0.2);">Liste des Categories</h1>

        <table class="table-auto w-full border-collapse border border-blue-200">
            <thead>
                <tr>
                    <th class="px-4 py-2 bg-blue-100 border-b">Id</th>
                    <th class="px-4 py-2 bg-blue-100 border-b">Nom</th>
                    <th class="px-4 py-2 bg-blue-100 border-b">Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for categorie in categories %}
                <tr>
                    <td class="px-4 py-2 border">{{ categorie.id }}</td>
                    <td class="px-4 py-2 border">{{ categorie.nom }}</td>
                    <td class="px-4 py-2 border">
                        <a href="{{ path('app_categorie_show', {'id': categorie.id}) }}" class="btn btn-primary">Voir</a>
                        <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="btn btn-secondary">Editer</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" class="px-4 py-2 border">Aucun enregistrement trouvé</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('app_categorie_new') }}" class="block mt-4 p-2 bg-blue-500 text-white text-center rounded hover:bg-blue-600">Créer une catégorie</a>
    </div>
{% endblock %}
