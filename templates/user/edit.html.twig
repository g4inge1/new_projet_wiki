
{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur{% endblock %}

{% block body %}
    <div class="container mx-auto mt-8">
        <h1 class="text-4xl font-bold mb-6 text-blue-800">Modifier l'Utilisateur</h1>

        {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a href="{{ path('app_user_index') }}" class="btn btn-secondary ml-2 mt-2 py-2 px-4">Retour à la liste</a>

        <div class="d-inline-block mx-auto mt-8">
            {{ include('user/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
